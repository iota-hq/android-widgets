<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. The real UI is Material 3 via Compose;
         this theme only styles the splash/window before Compose loads. -->
    <style name="Theme.PaperCut" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
