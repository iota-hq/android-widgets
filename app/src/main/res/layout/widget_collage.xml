<?xml version="1.0" encoding="utf-8"?>
<!-- Transparent widget: a single ImageView shows the pre-rendered collage
     bitmap so the torn cutouts appear to float on the wallpaper.

     The TextClocks on top are the one live element. RemoteViews can't animate,
     but the system ticks TextClock itself — no re-render, no alarm, no battery
     cost.

     Why six of them: RemoteViews can't set a Typeface at runtime, so each font
     is its own pre-declared view and the updater shows exactly one. Position is
     free-form: every clock fills the frame with gravity=center, and the updater
     shifts the centre with asymmetric view padding (setViewPadding is one of
     the few remotable layout calls). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <ImageView
        android:id="@+id/collage_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter"
        android:adjustViewBounds="true"
        android:contentDescription="@string/app_name" />

    <TextClock
        android:id="@+id/clock_classic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="sans-serif"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />

    <TextClock
        android:id="@+id/clock_thin"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="sans-serif-thin"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />

    <TextClock
        android:id="@+id/clock_condensed"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="sans-serif-condensed"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />

    <TextClock
        android:id="@+id/clock_serif"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="serif"
        android:textStyle="bold"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />

    <TextClock
        android:id="@+id/clock_mono"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="monospace"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />

    <TextClock
        android:id="@+id/clock_script"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:fontFamily="cursive"
        android:includeFontPadding="false"
        android:shadowColor="#80000000"
        android:shadowDx="0"
        android:shadowDy="2"
        android:shadowRadius="6"
        android:textColor="#FFFFFFFF"
        android:visibility="gone" />
</FrameLayout>
